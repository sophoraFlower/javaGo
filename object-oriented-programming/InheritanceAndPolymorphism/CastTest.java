package InheritanceAndPolymorphism;

public class CastTest {

    public static void main(String[] args) {
        /*
        Person p1 = new Student();
        Person p2 = new PhdStudent();
        PhdStudent phd1 = new Student();  // 父对象不可以当子对象来使用
        Teacher t1 = new Person();  // 父对象不可以当子对象来使用
        Student s1 = new PhdStudent();
        s1 = p1;  // 直接将父类的对象赋值给子对象，错误
        s1 = p2;  // 直接将父类的对象赋值给子对象，错误
        p1 = s1;
        t1 = s1;  // 非同一类型
        s1 = phd1;
        phd1 = s1;  // 直接将父类的对象赋值给子对象，错误
        */
        System.out.println("@@");
    }

}
